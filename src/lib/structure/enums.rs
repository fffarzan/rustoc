pub enum Direction {
    Left,
    Right,
    Up,
}

pub enum Color {
    Red,
    Yellow,
    Pink,
    Blue,
}

pub enum Flavor {
    Fruity,
    Sweet,
}

pub enum Access {
    Admin,
    Manager,
    User,
    Guest
}